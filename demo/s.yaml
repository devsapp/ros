edition: 1.0.0          #  命令行YAML规范版本，遵循语义化版本（Semantic Versioning）规范
name: rosApp            #  项目名称
access: default  #  秘钥别名

services:
  ros-test: #  服务名称
    component:  /Users/jiangyu/Desktop/ros/src
    props:
      region: cn-hangzhou
      name: test
      template: ./template.json
      parameters:
        VpcName: myvpcddd
        Description: testddddddddddddd